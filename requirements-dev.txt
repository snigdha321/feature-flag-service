# Development/test dependencies (kept in sync with pyproject.toml [dev] extra).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
aiosqlite>=0.20
ruff>=0.5
mypy>=1.10
